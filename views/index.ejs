<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <nav>
      <a href="/">Home</a>
      <a href="/new">New Message</a>
    </nav>
    <h1><%= title %></h1>

    <ul>
      <% messages.forEach(message => { %>
        <li>
          <h2><%= message.username %></h2>
          <p><%= message.messageText %></p>
          <b><%= message.creationDate %></b>

          <hr />
        </li>
      <%}) %>
    </ul>
  </body>
</html>
